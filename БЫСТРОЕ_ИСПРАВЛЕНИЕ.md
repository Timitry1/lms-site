# ⚡ Быстрое исправление проблемы синхронизации

## 🔴 Проблема
Вы создали **Static Site** на Render.com, но нужен **Web Service**!

## ✅ Решение за 5 минут

### 1. Создайте Web Service на Render.com

1. Зайдите на [render.com](https://render.com) → Dashboard
2. Нажмите **"New +"** → **"Web Service"** ⚠️ НЕ Static Site!
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: Free
5. Нажмите "Create Web Service"
6. Дождитесь развертывания (3-5 минут)

### 2. Скопируйте новый URL

После развертывания вы получите URL вида:
`https://lms-backend-XXXX.onrender.com`

### 3. Обновите script.js

Откройте `script.js`, найдите строку 4 и замените:

```javascript
const API_BASE = 'https://lms-site-1.onrender.com';
```

На новый URL:

```javascript
const API_BASE = 'https://ваш-новый-url.onrender.com';
```

### 4. Закоммитьте изменения

```bash
git add script.js
git commit -m "Обновлен URL backend"
git push
```

### 5. Проверьте работу

1. Откройте сайт
2. Нажмите F12 → Console
3. Должны увидеть: `✅ API_BASE установлен`
4. Должны увидеть: `✅ Данные успешно загружены`

## 🎯 Как проверить, что сервер работает?

Откройте в браузере:
- `https://ваш-url.onrender.com/` → должен вернуть JSON
- `https://ваш-url.onrender.com/sync` → должен вернуть JSON

Если видите JSON - всё работает! ✅

## ❌ Если видите "Not Found"

Это значит, что создан Static Site вместо Web Service. 
Удалите его и создайте заново как Web Service.

Подробная инструкция в файле: `ИНСТРУКЦИЯ_ПО_RENDER.md`
