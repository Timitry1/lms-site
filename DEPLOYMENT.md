# Инструкция по развертыванию LMS с синхронизацией данных

## Проблема
Данные хранятся только локально в браузере (localStorage), поэтому они не доступны с других устройств.

## Решение
Развернуть backend сервер на бесплатном хостинге и настроить автоматическую синхронизацию.

## Варианты бесплатного хостинга для Node.js

### 1. Render.com (рекомендуется - самый простой)

1. Зарегистрируйтесь на [render.com](https://render.com) (можно через GitHub)
2. Создайте новый "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name**: lms-backend (или любое другое)
   - **Environment**: Node
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: Free
5. Нажмите "Create Web Service"
6. Дождитесь развертывания (обычно 2-3 минуты)
7. Скопируйте URL вашего сервиса (например: `https://lms-backend.onrender.com`)

### 2. Railway.app

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект
3. Добавьте GitHub репозиторий
4. Railway автоматически определит Node.js проект
5. Скопируйте URL после развертывания

### 3. Fly.io

1. Установите Fly CLI: `npm install -g @fly/cli`
2. Зарегистрируйтесь: `fly auth signup`
3. В папке проекта: `fly launch`
4. Следуйте инструкциям

## Настройка фронтенда

После развертывания backend:

1. Откройте файл `script.js`
2. Найдите строку:
   ```javascript
   const API_BASE = '';
   ```
3. Замените на URL вашего backend сервера:
   ```javascript
   const API_BASE = 'https://your-backend-url.onrender.com';
   ```
   (Используйте URL, который вы получили при развертывании)

4. Сохраните файл и закоммитьте изменения в GitHub:
   ```bash
   git add script.js
   git commit -m "Настроена синхронизация с сервером"
   git push
   ```

## Как это работает

- **При загрузке страницы**: автоматически загружаются данные с сервера
- **При добавлении ученика/урока/роудмапа**: данные автоматически сохраняются на сервер
- **С любого устройства**: все данные синхронизируются через сервер

## Проверка работы

1. Откройте сайт на одном устройстве
2. Добавьте ученика или урок
3. Откройте сайт на другом устройстве
4. Данные должны автоматически появиться

## Важные замечания

⚠️ **Безопасность**: Текущий backend не имеет аутентификации. Для продакшена рекомендуется добавить:
- Аутентификацию пользователей
- HTTPS
- Защиту от спама/злоупотреблений

💾 **Хранение данных**: На бесплатных планах данные могут храниться в памяти. Для постоянного хранения:
- Используйте внешнюю базу данных (MongoDB Atlas, PostgreSQL)
- Или используйте файловое хранилище (S3, Cloudinary)

## Альтернативное решение (без backend)

Если вы не хотите развертывать backend, можно использовать:
- **Firebase** (бесплатный план)
- **Supabase** (бесплатный план)
- **MongoDB Atlas** (бесплатный план)

Но для этого потребуется переписать код для работы с этими сервисами.
